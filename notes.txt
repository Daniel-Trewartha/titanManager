TO ADD:

outputfiles: change to output execution string
make an output checker submission script in the same way as job submission script

campaign class: name, header, footer, checker header, checker footer
jobs have a campaign
only wraprun from same campaign

docs:
usage, samples




Campaign:
Should handle everything about its jobs
check for input files
submit X jobs to fill max N nodes
submit appropriately
resubmit if necessary